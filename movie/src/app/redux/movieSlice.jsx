import { createSlice } from '@reduxjs/toolkit'

export const movieSlice= createSlice({
name:'movie',
initialState:{
    
},
reducers : {

}
})
// export const {functions} = movieSlice.actions
export default movieSlice.reducer